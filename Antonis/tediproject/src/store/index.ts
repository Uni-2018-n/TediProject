import { createStore } from 'vuex'

export const store = createStore({
  state () {
    return {
      user: {
        // flag: Boolean,
        // token: String,
        // firstname: String,
        // lastname: String,
        // ProfilePic: String,
      },
    }
  }
})